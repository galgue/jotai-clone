import { ComplexAtom, PrimitiveAtom } from "./types";

const primitiveAtom = (initialValue: any): any => {
  return {};
};

const complexAtom = (reader: any): any => {
  return {};
};

export function atom(initialValueOrReader: any): any {}
